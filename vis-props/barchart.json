{
  "title": "Props of barchart(Vertical)",
  "type": "object",
  "properties": {
    "data": { "type": "string" },
    "x": {
      "type": "string",
      "description": "The attribute determining the x2 position of each bar."
    },
    "y": {
      "type": "string",
      "description": "The attribute determining the y position of each bar."
    },
    "selection": {
      "$ref": "../utils.json#/Table",
      "description": "the selected data items"
    },
    "color": {
      "$ref": "../utils.json#/Color",
      "description": "The attribute determining the color scheme of the bars."
    },
    "topK": {
      "type": "number",
      "description": "The number determining how many bars are shown at the top of the ranking based on the x attribute.",
      "default": 10
    }
  },
  "additionalProperties": false,
  "required": ["data"]
}
