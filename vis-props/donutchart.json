{
  "title": "Props of donutchart",
  "type": "object",
  "properties": {
    "data": { "type": "string" },
    "x": { "type": "string", "description": "the primary key of the data" },
    "y": { "type": "string", "description": "the value key of the data" },
    "selection": {
      "$ref": "../utils.json#/Table",
      "description": "the selected data items"
    }
  },
  "additionalProperties": false,
  "required": ["data", "x"]
}
