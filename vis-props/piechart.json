{
  "title": "Props of piechart",
  "type": "object",
  "properties": {
    "data": { "type": "string" },
    "color": {
      "$ref": "../utils.json#/Color",
      "description": "The attribute determining the color scheme of the bars."
    },
    "theta": {
      "type": "string",
      "description": "The attribute determining the theta angle of each pie."
    },
    "selection": {
      "$ref": "../utils.json#/Table",
      "description": "the selected data items"
    },
    "topK": {
      "type": "number",
      "description": "The number determining how many bars are shown at the top of the ranking based on the x attribute.",
      "default": 10
    }
  },
  "required": [
    "data",
    "range",
    "value",
    "aggregate",
    "count",
    "colors",
    "innerRadius",
    "selection"
  ],
  "additionalProperties": false
}
